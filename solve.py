from helpers import *

def solve_constraints(orig_constraints):
    constraints = orig_constraints.copy()
    
    
def canonical_form(orig_constraints):
    constraints = sorted( orig_constraints )
    
    return constraints